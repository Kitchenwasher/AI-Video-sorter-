# Keep your existing PyTorch install (ROCm/CUDA/CPU) and install this file first:
#   python -m pip install -r requirements.txt
#
# Then install facenet without dependency resolution so pip does not try to
# replace a working ROCm torch/torchvision setup:
#   python -m pip install facenet-pytorch==2.6.0 --no-deps
opencv-python>=4.10.0.84
numpy>=1.26.0
onnxruntime>=1.24.0
onnxruntime-directml>=1.24.0; platform_system=="Windows"
insightface>=0.7.3
scikit-learn>=1.5.0
tqdm>=4.66.4
